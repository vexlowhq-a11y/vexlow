# Nunca subir las API keys del panel de administración
admin/config.json

# Cache de Python
admin/__pycache__/
**/__pycache__/
*.pyc

# Basura del sistema operativo
.DS_Store
Thumbs.db

# Dependencias de node (Vercel las instala solo en el deploy)
node_modules/

# Documentos internos de estrategia — no van al repo público
docs/

# Borradores sin guardar de la vista previa en vivo del editor de niveles
admin/drafts/
